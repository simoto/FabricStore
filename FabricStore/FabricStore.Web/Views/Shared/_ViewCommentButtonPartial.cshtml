@model int
<div class="row blockquote-reverse">
    @using (Ajax.BeginForm("GetComments", new { Controller = "Products", area = string.Empty, id = Model },
                                new AjaxOptions
                                {
                                    HttpMethod = "Get",
                                    InsertionMode = InsertionMode.Replace,
                                    UpdateTargetId = "comments",                                   
                                }))
    {
        <input type="submit" class="btn btn-default" value="ShowComments" />
    }
</div>
